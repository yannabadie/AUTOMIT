# =============================================================================
# MOTHERSON IT AUTOMATION — Environment Variables
# Copy to .env and fill in your values
# =============================================================================

# --- Kestra ---
KESTRA_ADMIN_PASSWORD=Ch@ngeMeN0w!
POSTGRES_PASSWORD=kestra_secret_change_me

# --- Kestra Webhook Keys (generate unique keys) ---
# Use: openssl rand -hex 20
KESTRA_WEBHOOK_KEY=motherson-erp-wh-key-CHANGE-ME
WEBHOOK_KEY_ERP_RESTART=erp-restart-key-CHANGE-ME
WEBHOOK_KEY_HEALTH_CHECK=health-check-key-CHANGE-ME
WEBHOOK_KEY_ESCALATION=escalation-key-CHANGE-ME
WEBHOOK_KEY_AD_MAINTENANCE=ad-maintenance-key-CHANGE-ME
WEBHOOK_KEY_AD_ONBOARDING=ad-onboarding-key-CHANGE-ME
WEBHOOK_KEY_AD_OFFBOARDING=ad-offboarding-key-CHANGE-ME
WEBHOOK_KEY_SELFTEST=selftest-key-CHANGE-ME

# --- ZeroClaw ---
ZEROCLAW_BIN_PATH=/usr/local/bin/zeroclaw
ZEROCLAW_TOKEN=  # Generated at first pairing

# --- ZeroClaw Host Home (path to ~/.zeroclaw on Windows host) ---
ZEROCLAW_HOST_HOME=C:/Users/yann.abadie/.zeroclaw

# --- LLM Provider (choose one) ---
# Option A: Gemini CLI (cloud)
GEMINI_API_KEY=

# Option B: OpenRouter (cloud, multi-provider)
OPENROUTER_API_KEY=

# Option C: Ollama (local) — no key needed, just ensure ollama service is running
# Start with: docker compose --profile local-llm up -d ollama
# Then: docker exec ollama ollama pull qwen2.5-coder:14b

# --- Microsoft 365 (Graph API via Entra ID) ---
# App Registration: entra.microsoft.com > Identity > Applications > App registrations
# Permissions Application: User.Read.All, Directory.Read.All, AuditLog.Read.All,
#   Reports.Read.All, ServiceHealth.Read.All, SecurityEvents.Read.All,
#   IdentityRiskyUser.Read.All, Mail.Send (restreint via Application Access Policy)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# --- M365 Email (Graph API Mail.Send) ---
# Shared mailbox dedicee (pas de licence requise)
# Restreindre Mail.Send a cette boite via Application Access Policy Exchange Online
M365_SERVICE_MAILBOX=automit-noreply@adigroupe.onmicrosoft.com
M365_IT_TEAM_EMAIL=it-ops@adigroupe.onmicrosoft.com

# --- Teams Webhook ---
TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/YOUR-WEBHOOK-URL

# --- MCP Servers ---
# CEGID Oracle (JSON-RPC, 20 tools incl. query_database)
MCP_CEGID_ORACLE_URL=http://10.255.15.200:8000/mcp
MCP_CEGID_ORACLE_TOKEN=
# Sage X3 Oracle (Neo4j + vector search)
MCP_X3_ORACLE_URL=http://MAS_D0Z9TB4:8001/mcp/
MCP_X3_ORACLE_TOKEN=
# Also referenced as X3_MCP_URL/X3_MCP_TOKEN in some flows
X3_MCP_URL=http://MAS_D0Z9TB4:8001/mcp/
X3_MCP_TOKEN=

# --- Active Directory ---
AD_DOMAIN=ADGROUPE
AD_DC=
AD_SERVICE_ACCOUNT=ADGROUPE\svc_automit
AD_SERVICE_PASSWORD=

# --- VMware vSphere ---
VSPHERE_HOST=
VSPHERE_USER=
VSPHERE_PASSWORD=
VSPHERE_VERIFY_SSL=false
WEBHOOK_KEY_VMWARE_HEALTH=vmware-health-key-CHANGE-ME

# --- ServiceNow (optional, for L2 ticket creation) ---
SERVICENOW_API_URL=

# --- Monitoring (optional) ---
GRAFANA_PASSWORD=admin
